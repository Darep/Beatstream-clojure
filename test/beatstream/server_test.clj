(ns beatstream.server-test
  (:require [midje.sweet :refer :all]
            [beatstream.server :refer :all]))

;; Integration tests!

; TODO: figure out how I can call the route handler and see that it retuns the
; correnct HTTP Headers and Body
